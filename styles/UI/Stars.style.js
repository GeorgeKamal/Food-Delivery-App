import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  starContainer: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  star: {
    paddingEnd: 3,
  },
});

export default styles;
